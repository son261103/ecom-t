spring.application.name=ecom-t

# Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql-5f5f8ae-nguyenthanhchuc03012003-15d2.g.aivencloud.com}:${DB_PORT:27652}/${DB_NAME:defaultdb}?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_tkm5w122Ol28CLfA_PB}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:doqe3uw5k}
cloudinary.api-key=${CLOUDINARY_API_KEY:699148493824487}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:OT2KKPh-of-uMqmPKBFR70Y__JU}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server Configuration
server.port=${SERVER_PORT:8080}

spring.jmx.enabled=false

# Swagger Configuration - Single endpoint only
springdoc.swagger-ui.path=/docs

# Logging Configuration
logging.level.root=INFO
logging.level.org.hibernate=WARN
logging.level.sun.rmi=WARN
logging.level.javax.management.remote.rmi=WARN
logging.level.com.zaxxer.hikari=WARN
logging.level.org.springframework.data.jpa=WARN
logging.level.org.springframework.orm.jpa=WARN
logging.level.org.springframework.boot.autoconfigure=WARN
logging.level.jdk.event.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
