package com.example.ecomt.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Minimal configuration - let SpringDoc auto-configure
}
